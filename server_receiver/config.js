const options = {
	tempFolder: 	"../datafiles/temp/",	/* (optional) temp folder to stage files before moving to target folder */
	targetFolder: 	"../datafiles/output/",	/* folder where files are ultimately placed */
	serverPort: 	8888			/* port to listen on */
}

module.exports = options;
